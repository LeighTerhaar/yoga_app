<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All poses
    </h1>

    <a href="/poses/new" class="btn btn-block btn-outline-secondary">
      Add a new pose
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          English name
        </th>

        <th>
          Sanksrit
        </th>

        <th>
          Sequence
        </th>

        <th>
          Picture
        </th>

        <th>
          Sun a
        </th>

        <th>
          Sun b
        </th>

        <th>
          Sun c
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @poses.each do |pose| %>
      <tr>
        <!-- Display pose.id start -->
        <td>
          <%= pose.id %>
        </td>
        <!-- Display pose.id end -->

        <!-- Display pose.english_name start -->
        <td>
          <%= pose.english_name %>
        </td>
        <!-- Display pose.english_name end -->

        <!-- Display pose.sanksrit start -->
        <td>
          <%= pose.sanksrit %>
        </td>
        <!-- Display pose.sanksrit end -->

        <!-- Display pose.sequence_id start -->
        <td>
          <% if pose.flow.present? %>
            <a href="/yoga_flows/<%= pose.flow.id %>">
              <%= pose.flow.name %>
            </a>
          <% end %>
        </td>
        <!-- Display pose.sequence_id end -->

        <!-- Display pose.picture start -->
        <td>
          <%= pose.picture %>
        </td>
        <!-- Display pose.picture end -->

        <!-- Display pose.sun_a_id start -->
        <td>
          <% if pose.sun_a.present? %>
            <a href="/sun_as/<%= pose.sun_a.id %>">
              <%= pose.sun_a.created_at %>
            </a>
          <% end %>
        </td>
        <!-- Display pose.sun_a_id end -->

        <!-- Display pose.sun_b_id start -->
        <td>
          <% if pose.sun_b.present? %>
            <a href="/sun_bs/<%= pose.sun_b.id %>">
              <%= pose.sun_b.created_at %>
            </a>
          <% end %>
        </td>
        <!-- Display pose.sun_b_id end -->

        <!-- Display pose.sun_c_id start -->
        <td>
          <% if pose.sun_c.present? %>
            <a href="/sun_cs/<%= pose.sun_c.id %>">
              <%= pose.sun_c.created_at %>
            </a>
          <% end %>
        </td>
        <!-- Display pose.sun_c_id end -->

        <!-- Display pose.created_at start -->
        <td>
          <%= time_ago_in_words(pose.created_at) %> ago
        </td>
        <!-- Display pose.created_at end -->

        <!-- Display pose.updated_at start -->
        <td>
          <%= time_ago_in_words(pose.updated_at) %> ago
        </td>
        <!-- Display pose.updated_at end -->

        <td>
          <a href="/poses/<%= pose.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @poses, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
